import { useState } from 'react'; import GlobalModal from '../ui/GlobalModal';
export default function PromiseReviewDialog({ processing, onCancel, onConfirm }: { processing: boolean; onCancel: () => void; onConfirm: (note: string) => Promise<void> }) { const [note, setNote] = useState(''); const [error, setError] = useState(''); return <GlobalModal title="Reprovar promessa" onClose={onCancel} closeOnEscape={!processing} closeOnOverlay={!processing}><label className="text-sm font-bold">Observação<textarea aria-label="Observação" value={note} maxLength={500} onChange={event => setNote(event.target.value)} rows={5} className="mt-2 w-full rounded-xl border p-3" /></label>{error && <p role="alert" className="mt-2 text-red-600">{error}</p>}<div className="mt-5 flex justify-end gap-3"><button disabled={processing} onClick={onCancel} className="min-h-11 rounded-xl border px-5">Cancelar</button><button disabled={processing} onClick={() => { if (note.trim().length < 3) setError('Escreva pelo menos 3 caracteres.'); else void onConfirm(note).catch(() => setError('Não foi possível reprovar.')); }} className="min-h-11 rounded-xl bg-red-600 px-5 font-bold text-white">Reprovar</button></div></GlobalModal>; }

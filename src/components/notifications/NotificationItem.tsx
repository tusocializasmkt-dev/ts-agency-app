import { useNavigate } from 'react-router-dom';
import type { Notification } from '../../types';
import { isAllowedNotificationRoute } from '../../notifications';
export default function NotificationItem({ notification, onRead }: { notification: Notification; onRead: (id: string) => Promise<void> }) { const navigate = useNavigate(); const unread = !notification.readAt; const open = async () => { if (unread) try { await onRead(notification.id); } catch { /* A navegação permanece disponível. */ } if (notification.link && isAllowedNotificationRoute(notification.link)) navigate(notification.link); }; return <li className={`rounded-2xl border p-5 ${unread ? 'border-orange-200 bg-orange-50/50' : 'border-zinc-200 bg-white'}`}><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className="font-bold break-words">{notification.title}</h3>{unread && <span className="text-xs font-bold text-orange-700">Não lida</span>}</div><p className="mt-2 break-words text-sm text-zinc-600">{notification.message}</p></div><time className="shrink-0 text-xs text-zinc-500">{notification.createdAt?.toLocaleString('pt-BR') ?? 'Data indisponível'}</time></div>{notification.link && <button type="button" onClick={() => void open()} className="mt-4 min-h-11 rounded-xl bg-black px-4 text-sm font-bold text-white">Abrir</button>}{unread && !notification.link && <button type="button" onClick={() => void onRead(notification.id)} className="mt-4 min-h-11 rounded-xl border border-zinc-300 px-4 text-sm font-bold">Marcar como lida</button>}</li>; }

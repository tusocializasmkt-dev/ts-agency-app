import { useEffect, useState } from 'react'; import type { InvoiceHistory } from '../types'; import { watchInvoiceHistory } from '../services/invoices.service';
export function useInvoiceHistory(invoiceId: string, enabled: boolean) { const [history, setHistory] = useState<InvoiceHistory[]>([]); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null); useEffect(() => { if (!enabled) return; setLoading(true); return watchInvoiceHistory(invoiceId, data => { setHistory(data); setLoading(false); setError(null); }, () => { setError('Não foi possível carregar o histórico.'); setLoading(false); }); }, [invoiceId, enabled]); return { history, loading, error }; }

import type { BoletoPaymentResult, CardPaymentResult, CreatePaymentInput, PaymentResult, PixPaymentResult } from './payment.types';
export interface ParsedPaymentWebhook { providerEventId: string; providerPaymentId: string; eventType: string; }
export interface PaymentProvider { createPixPayment(input: CreatePaymentInput): Promise<PixPaymentResult>; createCardPayment(input: CreatePaymentInput): Promise<CardPaymentResult>; createBoletoPayment(input: CreatePaymentInput): Promise<BoletoPaymentResult>; getPaymentStatus(providerPaymentId: string): Promise<PaymentResult>; cancelPayment(providerPaymentId: string): Promise<PaymentResult>; refundPayment(providerPaymentId: string): Promise<PaymentResult>; parseWebhook(payload: unknown): Promise<ParsedPaymentWebhook>; verifyWebhook(payload: unknown, signature?: string): Promise<boolean>; }

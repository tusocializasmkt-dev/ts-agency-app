export type PaymentErrorCode = 'payment-provider-unavailable' | 'payment-provider-not-configured' | 'payment-rejected' | 'invalid-payment-method' | 'payment-expired' | 'invoice-already-paid' | 'invoice-cancelled' | 'duplicate-payment' | 'invalid-card-token' | 'webhook-validation-failed' | 'provider-timeout' | 'backend-required';
const messages: Record<PaymentErrorCode, string> = { 'payment-provider-unavailable': 'O serviço de pagamento está indisponível.', 'payment-provider-not-configured': 'O provedor de pagamento ainda não está configurado.', 'payment-rejected': 'O pagamento não foi aprovado.', 'invalid-payment-method': 'A forma de pagamento é inválida.', 'payment-expired': 'O pagamento expirou.', 'invoice-already-paid': 'Esta fatura já foi paga.', 'invoice-cancelled': 'Esta fatura foi cancelada.', 'duplicate-payment': 'Foi detectado um possível pagamento duplicado.', 'invalid-card-token': 'Não foi possível validar os dados tokenizados do cartão.', 'webhook-validation-failed': 'Não foi possível validar o evento de pagamento.', 'provider-timeout': 'O provedor demorou para responder.', 'backend-required': 'Esta operação exige o backend financeiro seguro.' };
export class PaymentError extends Error { constructor(public readonly code: PaymentErrorCode) { super(messages[code]); this.name = 'PaymentError'; } }
export function mapPaymentError(cause: unknown): PaymentError { return cause instanceof PaymentError ? cause : new PaymentError('payment-provider-unavailable'); }

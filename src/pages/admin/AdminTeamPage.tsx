import { useState } from 'react'; import { KeyRound, Plus } from 'lucide-react'; import { useBrands, useFeedback, useTeamMembers } from '../../hooks'; import type { TeamMember } from '../../types'; import TeamMemberDialog, { type TeamMemberForm } from '../../components/Admin/TeamMemberDialog'; import PasswordDialog from '../../components/auth/PasswordDialog'; import { callCreateTeamMember, callResetTeamMemberPassword, callUpdateTeamMember } from '../../data/functions';
export default function AdminTeamPage() { const { members, loading, error } = useTeamMembers(); const { brands } = useBrands(); const feedback = useFeedback(); const [editing, setEditing] = useState<TeamMember | null | undefined>(); const [reset, setReset] = useState<TeamMember | null>(null); const [processing, setProcessing] = useState(false); const save = async (data: TeamMemberForm) => { setProcessing(true); try { if (editing) await callUpdateTeamMember({ uid: editing.uid, ...data }); else await callCreateTeamMember(data); feedback.success(editing ? 'Membro atualizado.' : 'Membro criado.'); setEditing(undefined); } finally { setProcessing(false); } }; if (loading) return <p>Carregando equipe...</p>; if (error) return <p className="text-red-600">{error}</p>; return <div className="space-y-7"><header className="flex items-center justify-between"><div><h1 className="text-3xl font-bold">Equipe</h1><p className="text-zinc-500">Gerencie funções e clientes permitidos.</p></div><button onClick={() => setEditing(null)} className="rounded-xl bg-black px-5 py-3 font-bold text-white"><Plus className="mr-2 inline h-4 w-4" />Novo membro</button></header><div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-zinc-50 text-xs uppercase text-zinc-500"><tr><th className="p-4">Nome</th><th>E-mail</th><th>Função</th><th>Status</th><th>Clientes</th><th>Ações</th></tr></thead><tbody>{members.map(member => <tr key={member.id} className="border-t"><td className="p-4 font-bold">{member.displayName}</td><td>{member.email}</td><td>{member.role === 'manager' ? 'Gerente' : 'Social Media'}</td><td>{member.active ? 'Ativo' : 'Inativo'}</td><td>{member.brandIds.map(id => brands.find(b => b.id === id)?.name ?? id).join(', ') || 'Nenhum'}</td><td><button onClick={() => setEditing(member)} className="mr-3 font-bold underline">Editar</button><button aria-label={`Redefinir senha de ${member.displayName}`} onClick={() => setReset(member)}><KeyRound className="h-4 w-4" /></button></td></tr>)}</tbody></table></div>{editing !== undefined && <TeamMemberDialog member={editing} brands={brands} processing={processing} onClose={() => setEditing(undefined)} onSave={save} />}{reset && <PasswordDialog title={`Redefinir senha de ${reset.displayName}`} processing={processing} onClose={() => setReset(null)} onConfirm={async password => { setProcessing(true); try { await callResetTeamMemberPassword(reset.uid, password); feedback.success('Senha atualizada.'); setReset(null); } finally { setProcessing(false); } }} />}</div>; }

{
  "name": "ts-agency-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "build": "tsc",
    "test": "npm run build && node --test lib/test/*.test.js",
    "seed:auth:emulator": "npm run build && node scripts/seed-internal-auth.mjs",
    "grant:storage-admin": "node scripts/grant-storage-admin.mjs"
  },
  "dependencies": {
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.2"
  },
  "devDependencies": {
    "@types/node": "22.14.0",
    "typescript": "5.8.2"
  }
}
